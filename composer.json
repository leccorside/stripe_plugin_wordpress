{
    "name": "donation-stripe/plugin",
    "description": "Plugin WordPress de doações com Stripe (cartão, boleto, único e recorrente).",
    "type": "project",
    "require": {
        "php": ">=8.0",
        "stripe/stripe-php": "^10.0"
    },
    "autoload": {
        "classmap": []
    },
    "config": {
        "optimize-autoloader": true
    }
}
